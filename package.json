{
  "name": "patchboot-example-app",
  "version": "1.0.0",
  "scripts": {
    "run:browser": "open-cli \"http://localhost:8080\" ",
    "run:server": "http-server . -p 8080",
    "run": "run-p run:*",
    "deploy-app": "patchboot-install FeedApp.js 'Feed App' 'An app deployed with patchboot-install'"
  },
  "devDependencies": {
    "npm-run-all": "4.1.5",
    "open-cli": "6.0.1",
    "patchboot-install": "0.0.1"
  }
}
